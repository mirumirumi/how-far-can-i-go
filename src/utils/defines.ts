export interface LatLng {
  lat: number,
  lng: number,
}

export enum SystemThemeConfig {
  LIGHT = "light",
  DARK = "dark",
  OS_Sync = "os",
}

export enum Transportation {
  WALKING = "walking",
  CYCLING = "cycling",
  DRIVING = "driving",
}
